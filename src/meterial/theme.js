import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    fontFamily: "Vazirmatn",
  },
  direction: "rtl",
  palette: {
    dark: {
      black: "#212529",
      white: "#f8f9fa",
    },
  },
});
export default theme;
